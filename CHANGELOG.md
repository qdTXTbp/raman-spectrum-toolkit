# 更新日志 · Changelog

## v2.5

* **图例不再压谱线，位置也由你定**（针对“叠加图和其它图片的图例在谱线密集时会遮挡谱线”）。
  以前图例固定画在**绘图区里的右上角**，靠一层白底卡片“盖住但读得清”——
  堆叠图上曲线从下到上铺满整幅，图例必然压掉一块谱线。
  现在图例一律画在**绘图区外面**，横向与谱线错开，谱线再密也压不到：
  * **位置可选**：右侧留白（默认）/ 上方 / 下方 / 图内自由位置 / 不显示。
    选“上方”“下方”时会把绘图区、标题和横坐标标题一起让开，互不叠字。
  * **可以直接拖动**：在叠加图预览窗口里按住图例拖到任意位置，松手即定位；
    按在图上别处仍然是“加峰位”，两种操作互不干扰（拖动时显示虚线预览框）。
  * **位置会记住**：拖好（或在【设置 → 高级设置 → 图幅与图例】里选好）之后写进
    设置文件，导出 PNG 和下次预览都用同一个位置。
  * **条目数不再被砍**：以前“图例只列前 12 条”，20 条谱线就有 8 条没有图例对不上号
    （这句话也一并从说明书里删掉了）。现在**几条谱线就列几条**，
    竖着放不下就自动分列（最多 4 列），按需把标签截短以保住绘图区宽度；
    实在放不下才省略，并在图例下方明确写出“还有 N 条没列”。
  * 图例位置对**所有出图**生效：叠加图、瀑布图、鉴定对比图、配对对比报告、
    主界面预览，以及命令行 `--legend-pos right|top|bottom|inside|none`。
  * 修掉一个连带 bug：配对对比报告选“下方”时，排名表往下让开了，
    但“最佳配对峰位对照”表没有跟着让，两张表会叠字；现在两张表一起下移。
  * 新增回归自测 `tests/test_legend_layout.py`（62 项）：逐项检查
    right/top/bottom 的图例框与绘图区**零重叠**、inside 落在图内且听比例坐标、
    40 条谱线自动分列后条目齐全且绘图区还留得下、拖动往返位置不漂、
    图例位置能存能读能还原、配对报告两张表不重叠、主界面预览的图例也在绘图区外，
    并在真实 Tk 窗口里**模拟一次鼠标拖动**，验证位置被写进设置且导出沿用同一位置。

## v2.4

* **下载数据库不再“崩溃”也不再白等**（针对“总是崩溃和下载缓慢”）。
  先实测定位了原因：rruff.net 单连接只有约 **0.09 MB/s** 且速度还会衰减，
  多路并发能明显提速；227 MB 的包按旧实现要 40 多分钟，
  而旧的下载代码**全程把界面堵死**。
  * **界面不再假死**：下载改为后台线程 + 真进度条 + 已下/总数 + 实时速率 +
    剩余时间 + **【取消】按钮**。旧代码在下载期间只调 `update_idletasks()`，
    不处理鼠标和关闭事件，整段时间窗口是假死的，用户只能强杀进程。
  * **断点续传**：服务器支持 `Accept-Ranges: bytes`（Range 请求返回 206），
    现在按分片写 `.part0/.part1…`。**取消、掉线、强杀都不会白费**，
    下次接着下；旧实现不支持 Range，一断就从 0 重来。
  * **并发分片**：默认 32 路（1~128 可调，小于 2 MB 的文件自动单连接）。
    实测同一时段：1 路 0.027、8 路 0.32、32 路 0.89、64 路 1.28、96 路 1.45 MB/s，
    服务器全程没有拒绝，所以并发数往上给是实打实的提速。
  * **动态领活（work-stealing）**：分片不再“一条连接固定一段、平均分”。
    实测 33 MB 的包 32 段里**最快段 4.3 s、最慢段 79 s**，
    静态均分时总耗时等于最慢那一段（95% 的时间在等一条卡住的连接）；
    现在把文件切成“工作单元”放进队列，**谁下完谁再领下一段**，
    快连接会把剩下的活一起吃掉，慢尾被摊平。
    真实链路同一时段交错 A/B（12 MB 的包、都是 32 路）：
    静态均分 32.7 s / 45.5 s（平均 39.1 s）→ 动态领活 22.1 s / 24.3 s（平均 23.2 s），
    **快 1.69 倍**。
    单元数写进 `.part.meta`，续传时沿用上次的切法，中途改并发数也不会让分片错位。
  * **代理支持**：可选填代理地址（或直接沿用 Windows 系统代理），
    这条跨境链路丢包严重（实测 ping 丢包 25%、RTT 236 ms），
    走代理/VPN 往往比堆并发快得多。命令行 `--proxy` / `--dl-conns` 也可调。
  * **自动重试**：分片内失败按 1.5×n 秒退避重试，并从该分片断点继续；
    服务器返回 429/503 会识别出来并提示“把并发调小些”。
  * **完整性校验（修掉一个真 bug）**：旧代码读了 `Content-Length` 却只用于显示，
    `os.replace()` 无条件执行——**传输被截断的 zip 会被当成“下载成功”并标记“已下载”**，
    直到建索引时才抛 BadZipFile，看起来就像又崩了一次。现在长度对不上就不转正、
    保留分片供续传；下完再验一次 zip 能否打开，损坏的自动删除并提示重试。
  * **失败清理**：异常中断不再留下没人管的残file；`.partN` 分片本身就是断点，
    不会被当成正式数据包。
  * ROD 参考谱路径同步加固：网络请求带重试与体积上限（原来是无上限整体读进内存）、
    落库改为**原子写**（原来直接写最终文件，半截 CSV 会被 `db_list` 当成正常谱进库）、
    并按 JDX 的 `NPOINTS` 判断是否被截断。
* **数据包列表新增「预计耗时」列**：按**你上次实测到的速率**估算（会越用越准），
  并在下 ≥50 MB 的大包前弹窗告知预计时长；下载期间窗口可正常操作、随时取消。
* 修复：RRUFF 数据包对话框的表头（状态/类型/数据包/大小/索引条数）在英文模式下
  没有翻译。
* 新增回归自测 `tests/test_download_engine.py`（45 项）：用一个可“说谎”的本机
  HTTP 服务器复现真实故障——传一半断线、`Content-Length` 报全长却只发一半、
  完全不支持 Range——逐项验证续传（累计只传 1 倍而不是 2 倍）、跨运行续传、
  改了并发数续传仍能拼出正确文件（切法记在 `.part.meta` 里）、
  工作单元首尾相接无缝无重叠、动态领活能领完全部单元、
  截断必须报错且**不生成目标文件**、取消及时且线程不抛未捕获异常。
* 说明书版本升至 2.4，中英两份同步更新。

## v2.3

* **新增「批量配对」**：一次拿一整个文件夹的实测谱去配一组参考谱，
  专门用来找“对不上的那几条”。
  * 界面：【分析工具 → 配对比较（手动 / 自动）→ 批量配对（文件夹 × 参考谱）…】
  * 命令行：`--pair-batch 文件夹 [--pair-ref 参考谱文件夹]`
  * 实测谱取一个文件夹（留空则用主界面左侧选中的光谱）；参考谱取
    本地参考谱库，或自己指定的文件夹 / 文件
  * 汇总表**按综合分升序排**：对不上的排在最前面，一眼看到可疑的
  * 典型用法：一批锆石 × 锆石标准谱，综合分低的就是“可能混了别的晶”的
* **新增「批量鉴定」**：一堆不认识的谱，逐条在全库里找最像的矿物。
  * 界面：【分析工具 → 批量鉴定（文件夹逐条鉴定）…】
  * 命令行：`--identify-batch 文件夹 [--identify-top 5]`
  * 每条给出最佳候选、综合分、F1 与结论文本
* **汇总表只给辅助数据，不下最终结论**（按你的要求）：
  * 综合分（= 0.5×F1 + 0.5×强峰命中率，与「未知谱鉴定」**同一口径**，
    所以两处的数字对得上）、F1、强峰命中、命中 / 实测 / 参考峰数、
    平均偏差、相关系数、谱角、偶然概率、领先第二名（分）
  * 「参考判读」一列只按分数给提示（匹配良好 / 部分匹配 / 匹配很差），
    **最终是哪个物相由你自己核对峰位与谱型来判**
* **输出**：汇总表 `CSV`（一条谱一行）+ 汇总报告 `HTML`（浏览器可直接打印为 PDF），
  都放在“工具数据/分析结果”。
* 配对与鉴定统一到同一套打分口径（`score_peaks_vs_ref`），
  不再出现“配对报告里的 F1”与“鉴定里的综合分”各说各话。
* 修复：鉴定结论里“仅领先第二名”偶尔显示负数（精算后排序键换成 F1，
  第 2 名的综合分可能反而更高）——现在夹到 0，不再出现“仅领先 -14 分”。
* 新增回归自测 `tests/test_batch_pair_identify.py`（38 项）：
  自配满分、异类低分、可疑的排最前、汇总表列数对齐、CSV / HTML 落盘、
  英文模式无中文残留，以及两个对话框的开窗 / 跑批 / 结果进表。
* 说明书版本升至 2.3，中英两份同步更新。

## v2.2

* **「多数据图叠加」改成「先预览、确认后再导出」**：
  * 打开后先出**预览窗口**，改参数、增减峰位都只重画预览，
    **不点【导出 PNG】就不会往结果目录写任何文件**（旧版一点就落盘）
  * **左键点图 = 在点击处加一个峰位**（蓝色虚线 + 蓝色数值）
  * **右键点虚线 = 删掉离点击处最近的峰位**（自动检测的、手动加的都能删）
  * 【重新检测】丢掉全部手动改动，回到自动检测的峰位
  * 参数改完按回车或点【刷新预览】才重画，免得每敲一个字符就重绘一次
  * 预览图按屏幕高度自适应缩放，窗口高度不超过屏幕可用高度，小屏也放得下
  * 命令行 `--overlay` 仍是批处理，没有预览窗口，直接按自动检测的峰位出图
* 新增回归自测 `tests/test_overlay_preview.py`（19 项）：在真实 Tk 窗口里
  走一遍左键加峰 / 右键删峰 / 重新检测 / 确认后导出，
  并断言“还没点导出时输出目录必须是空的”。
* 说明书版本升至 2.2，中英两份同步更新（`使用说明.txt` / `User_Guide.txt`）。

## v2.1

* **新增「多数据图叠加」**：把所选的多条光谱（或一个文件夹里的全部光谱）
  叠画在同一套坐标轴上，**每个数据集一种颜色**。
  * 界面：菜单【分析工具 → 多数据图叠加（所选光谱）…】
  * 命令行：`--overlay 文件夹`
  * **按 stacked spectra 排布**：各条先归一化到最大值 = 1，再按“谱线偏移”
    纵向错开 k×偏移，**谱线彼此分开、不压在一起**（偏移 1.0 = 刚好不压线）
  * **峰位跨谱合并**：同一个峰在每条谱上都标一遍会糊成一片，现在把邻近的峰
    归成同一个峰，**只画一条虚线、只标一个平均波数**（容差可调，
    留空沿用“最小峰间距”；命令行 `--peak-merge 30` / `--no-peak-merge`）；
    各条谱自己的峰位标记仍保留，能看出是哪几条谱贡献的
  * 颜色自动分配：前 8 条用标准色板，超过 8 条按黄金角旋转色相生成新色，
    60 条以内不重色
  * **横坐标取各条谱波数范围的交集**，某条谱短一截时右边不再空出一段白；
    若在【高级设置】里手填过横坐标范围，则以手填的为准
  * **可隐藏峰位数值**：取消勾选后只留峰位标记与虚线，十几条谱叠在一起时更清爽
  * 右侧图例标出来源文件名（最多 12 条）并带白底，压在谱线上也读得清
  * **导出**为 `叠加图_N条.png`，存放到分析结果目录（v2.2 起改为点【导出 PNG】才落盘）
* **峰位波长虚线**：自动识别的峰和手动补标的峰，都会从峰顶画一条虚线
  引到横坐标轴，峰对应的波数一眼可读。界面预览与导出的 PNG 都生效。
  可在【高级设置 → 峰识别与峰拟合】里关掉，命令行用 `--no-peak-dash`。
* **可以删除自动标注的峰**：右键对准某个峰即可删掉离鼠标最近的那个峰，
  自动峰与手动峰都适用。自动峰记进该文件的“已删除”名单，
  出图、峰列表与所有导出都按删除后的结果计算；
  点【恢复自动峰】一次全恢复（菜单【设置】→【恢复被删的自动峰】）。
  为避免误删，要求点在峰附近（横向约 ±1/40 图宽），
  在空白处右键只会提示“附近没有峰”。
* **显示峰位数值**开关：主界面【图表设置】里可关掉峰位数字（标记与虚线保留），
  命令行用 `--no-peak-labels`。
* **修复：高级设置窗口超出屏幕**。小屏 / 高 DPI 下原窗口会把底部
  「确定 / 应用 / 取消」顶到屏幕外，点不到。现在内容改为可滚动，
  按钮固定在底部，窗口高度不超过屏幕可用高度。
* 修复：命令行瀑布图纵轴标签在英文模式下没有翻译。
* 说明书版本升至 2.1，中英两份同步更新。

## v2.0 — 拉曼光谱工具 · Raman Spectrum Toolkit

* **正式命名**：中文「拉曼光谱工具」，English “Raman Spectrum Toolkit”；
  副题「转换 · 分析 · 鉴定 / Convert · Analyze · Identify」。
* **新增应用图标**，并写入 exe 文件图标、窗口与任务栏。
* **全工具中英双语**：界面、菜单、对话框、日志、命令行输出、分析报告、
  图片图注与说明书都可切换（菜单【设置 → 语言】或 `--lang en|zh`）；
  翻译表 1000+ 条，支持“先格式化再输出”的模板回退翻译。
* **说明书中英各一份**：`使用说明.txt` / `User_Guide.txt`，
  工具内可实时切换语言阅读，章节跳转 + 关键词搜索 + 字号可调。
* **未知光谱全库鉴定**：参考库特征峰索引 + 两级检索，
  综合分 / 强峰命中 / 领先幅度三重判据，给出偶然概率与峰位对照。
* **元素筛选**：`--identify-must "Zr Si"`、`--identify-not Ca`，可对接 EDS 结果缩小范围。
* **数据目录统一**到工具目录的 `工具数据/`（参考谱库 / RRUFF数据包 / 分析结果），
  不再散落到其它磁盘。
* 修复：命令行若干提示漏译、配对报告分隔行漏译、鉴定结论英文标点缺空格、
  瀑布图路径打印残留调试串、只读目录回退路径等。

## v1.x — 早期版本

* JASCO `.jws` → CSV / Excel（含图表）/ PNG 转换，OLE2 结构解析与逐点校验。
* 峰识别与手动补标、峰拟合（高斯 / 洛伦兹 / 伪 Voigt）、峰位检索、相似度矩阵。
* ROD 在线检索、RRUFF 数据包下载 / 按矿物检索 / 导出本地库、内置矿物特征峰归属表。
* 配对比较（手动 / 自动）、聚类分析 + PCA、二维成像热图、谱运算、瀑布图。
* 整目录批处理 + 汇总表、自包含 HTML 分析报告、数据文件夹与容量管理。
